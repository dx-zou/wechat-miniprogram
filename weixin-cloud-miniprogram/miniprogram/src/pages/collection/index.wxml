
<view class="index-container">
  <view class="nav-wrapper" wx:for="{{list}}" wx:key="name">
    <navigator url="{{item.url}}" open-type="navigate" class="nav-inner">
      <text>{{item.name}}</text>
    </navigator>
  </view>
</view>
